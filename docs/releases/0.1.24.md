## 本次更新

- GitHub Release 改为读取版本专属的中文更新摘要，不再混入自动生成的英文更新列表。
- 发布前会校验 `docs/releases/<version>.md` 存在、内容非空且包含中文，避免发布缺少版本说明的安装包。
- Release 正文会统一附带 fnOS、macOS 与 `SHA256SUMS` 的下载说明，以及未签名、未公证 macOS 镜像的安全提示。
