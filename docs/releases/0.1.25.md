## 本次更新

- 优化长文档渲染链路：复用 Markdown 解析快照、按最新内容刷新预览，并通过渐进式渲染降低大文档切换与编辑时的卡顿。
- 增强代码高亮调度：支持取消过期任务、复用并缓存高亮结果，减少频繁切换文件时的资源浪费与界面抖动。
- 统一 fnOS 与 macOS 的安全保存结果契约，补充乐观锁、原子写入、冲突与恢复场景测试。
- 新增跨端 Markdown 渲染契约，覆盖 KaTeX、Mermaid、代码高亮、图片安全与查找等边界情况，并接入 CI。
- 改进 macOS 降级体验：原生首屏占位会在 Web 内容真正完成绘制后无感接管，同时保留超时降级与运行时回滚能力。
