#!/bin/bash
#
# 安装完成后调用。
#
# 本应用无需在此阶段做额外处理：
#   - 依赖（Node.js 运行时）由 manifest 的 install_dep_apps 声明，系统负责准备
#   - 服务启停由 cmd/main 管理
#   - 文件授权由管理员在 fnOS 应用设置的「访问权限」中统一配置
# 因此直接返回成功。若后续需要迁移数据或清理残留，在此补充即可。

exit 0
