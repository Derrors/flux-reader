## 本次更新

- 重新设计 macOS 版界面，引入更统一的 Liquid Glass 视觉语言，优化标题栏、工具栏、侧边栏、标签页与正文容器的层次和质感。
- 收窄并统一正文外层与纸张区域四周留白，让阅读区域更紧凑、内容对齐更自然。
- 优化右侧文档目录的自动隐藏效果，折叠后仅保留轻量的展开图标，减少对正文空间的占用。
- 修复关闭最后一个 macOS 窗口后应用仍留在后台的问题；有未保存内容或保存任务时仍会先执行安全确认。
