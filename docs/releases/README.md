# Platform release notes

Flux Reader releases each platform independently. Add one Chinese release note
for the platform being published:

```text
docs/releases/fnos/<version>.md
docs/releases/macos/<version>.md
docs/releases/windows/<version>.md
```

The historical files directly under `docs/releases/` belong to the former
single-version release process and are retained for reference only.
